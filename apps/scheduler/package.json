{
  "name": "@frodo/scheduler",
  "version": "0.1.0",
  "private": true,
  "description": "Cron tick client for Railway @frodo/scheduler",
  "type": "module",
  "scripts": {
    "prebuild": "pnpm --filter @frodo/config build",
    "start": "node dist/tick.js",
    "dev": "tsx src/tick.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@frodo/config": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
